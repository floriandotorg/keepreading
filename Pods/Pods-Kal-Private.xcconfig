#include "Pods-Kal.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Kal" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Kal" "${PODS_ROOT}/Headers/ZBarSDK"
OTHER_LDFLAGS = -ObjC ${PODS_KAL_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}